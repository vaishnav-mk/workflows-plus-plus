{
  "name": "workflows-backend-tests",
  "version": "1.0.0",
  "description": "Tests for workflows-backend",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:ui": "vitest --ui"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.9.0",
    "vitest": "^3.2.0",
    "@cloudflare/workers-types": "^4.20231218.0"
  }
}

